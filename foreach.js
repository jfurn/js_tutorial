let a = ["ant", "bat", "cat", 42];
a.forEach(function(element) {
  console.log(element);
});


let soliloquy = "To be, or not to be, that is the question:";
Array.from(soliloquy).forEach(function(character) {
  console.log(character);
});


/*alternative syntax but couldn't figure this one out
let altSoliloquy = forEach(soliloquy.character => {
  console.log(character);
}
);*/


let c = [99, 17, 42, 8];
c.sort(function(a, b) {
    return a - b;
    }
  );

c.forEach(function(console.log(sort(function(a, b))) {
      return a - b;
      }
    );

console.log(c.sort());
